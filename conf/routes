# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
POST    /onSales                    controllers.ItemItem.onSales
POST    /recommendByItem            controllers.RecommendByItem.byItem
POST    /createItemRecommendBySite  controllers.ItemRecommendBySite.create
POST    /listItemRecommendBySite    controllers.ItemRecommendBySite.list
POST    /recommendBySiteItem        controllers.ItemRecommendBySite.byItem

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
